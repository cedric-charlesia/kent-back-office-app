import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kent-back-ofice-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
